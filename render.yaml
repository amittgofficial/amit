services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python code.py"
    autoDeploy: true
